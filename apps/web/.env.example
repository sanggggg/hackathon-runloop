# The API keeps a shared bearer token. It must never reach the browser, so this
# is deliberately not a NEXT_PUBLIC_ variable — every call goes through a route
# handler or a server component.
BRANCHPOINT_API_URL=http://localhost:4000
BRANCHPOINT_API_TOKEN=replace-me
